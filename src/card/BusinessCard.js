// title : string
// photo : string
// info  : object
class BusinessCard {
    constructor(title, photo, info){
        this.title = title;
        this.photo = photo;
        this.info = info;
    }
};

export default BusinessCard;